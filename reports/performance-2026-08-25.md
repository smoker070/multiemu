# Performance baseline

Generated by `multiemu-perf`. The QEMU command line came from
`QEMUCommandBuilder`, so these describe the configuration the product ships.

Guest services: stopped vendor.ril-daemon, vendor.threadnetwork_hal.
Stopping services this host cannot support is what `QEMUBackend` does on its own
boot-completed transition; a measurement without it describes a guest the
product would not have left in that state.

| Metric | Value | Target | Verdict |
| --- | --- | --- | --- |
| Cold boot to `sys.boot_completed` | 3.706 s | <= 45 s (60 s max) | **PASS** |
| Idle host CPU, all processes | mean 1.5% of one core, p95 8.8% | < 10% | **PASS** |
| Peak resident memory, all processes | 1006.5 MiB | none stated | — |

## Frame rate and pacing

Sampled for 60 s after a 120 s settle, from frames
delivered over the D-Bus display channel. Guest mode as actually rendered:
1920x1920 — `SetUIInfo` advises, and the guest decides,
so this may differ from the mode requested.
| Statistic | Value |
| --- | --- |
| Frames observed | 2343 |
| Mean rate | 37.1 FPS |
| p50 frame interval | 26.39 ms |
| p95 frame interval | 40.67 ms |
| p99 frame interval | 45.15 ms |
| Longest interval | 555.07 ms |
| Intervals over 33.3 ms | 235 |
| Intervals over 16.7 ms | 2145 |

Pacing: p99 45.15 ms against 2x p50 52.77 ms — **PASS**.

Sustained rate: 37.1 FPS against >= 30 — **PASS**.
Workload: 356 swipes driven over ADB during sampling.

## Per-process attribution

| Process | Mean CPU (% of one core) | p95 CPU | Peak RSS |
| --- | --- | --- | --- |
| `qemu-system-aarch64` | 7.4 | 18.0 | 1001.6 MiB |
| `multiemu-guest-service` | 0.0 | 3.6 | 4.9 MiB |

Samples: 265, one per second.

## Not measured here

- **Input latency** — needs a guest-side timestamp to correlate against, which
  needs input delivery verified inside the guest.
- **APK installation** — measured by Milestone 10 against package size, not by
  this harness, because it is a transfer rate rather than a steady-state cost.
- **Disk I/O and snapshot duration** — measured by their own milestones' spikes,
  not by this harness.