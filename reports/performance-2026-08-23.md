# Performance baseline

Generated by `multiemu-perf`. The QEMU command line came from
`QEMUCommandBuilder`, so these describe the configuration the product ships.

| Metric | Value | Target | Verdict |
| --- | --- | --- | --- |
| Cold boot to `sys.boot_completed` | 3.748 s | <= 45 s (60 s max) | **PASS** |
| Idle host CPU, all processes | mean 10.2% of one core, p95 20.3% | < 10% | **FAIL** |
| Peak resident memory, all processes | 1.29 GiB | none stated | — |

## Frame rate and pacing

Sampled for 60 s after a 90 s settle, from frames
delivered over the D-Bus display channel. Guest mode as actually rendered:
1920x1920 — `SetUIInfo` advises, and the guest decides,
so this may differ from the mode requested.
| Statistic | Value |
| --- | --- |
| Frames observed | 2607 |
| Mean rate | 39.1 FPS |
| p50 frame interval | 25.46 ms |
| p95 frame interval | 41.62 ms |
| p99 frame interval | 46.12 ms |
| Longest interval | 599.03 ms |
| Intervals over 33.3 ms | 314 |
| Intervals over 16.7 ms | 2102 |

Pacing: p99 46.12 ms against 2x p50 50.93 ms — **PASS**.

Sustained rate: 39.1 FPS against >= 30 — **PASS**.
Workload: 382 swipes driven over ADB during sampling.

## Per-process attribution

| Process | Mean CPU (% of one core) | p95 CPU | Peak RSS |
| --- | --- | --- | --- |
| `qemu-system-aarch64` | 10.2 | 20.3 | 1.29 GiB |

Samples: 53, one per second.

## Not measured here

- **Input latency** — needs a guest-side timestamp to correlate against, which
  needs input delivery verified inside the guest.
- **APK installation** — M10 is not implemented.
- **Disk I/O and snapshot duration** — measured by their own milestones' spikes,
  not by this harness.